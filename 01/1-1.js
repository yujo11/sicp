/**
 * 1-1. 다음은 일련의 문장이다. 각 문장에 대해 해석기가 출력하는 결과는 무엇인가?
 */

10; // 10

5 + 3 + 4; // 12

9 - 1; // 8

6 / 2; // 3

2 * 4 + (4 - 6); // 6

const a = 3; // undefined

const b = a + 1; // undefined

a + b + a * b; // 19

a === b; // false

b > a && b < a * b ? b : a; //  b === 4

a === 4 ? 6 : b === 4 ? 6 + 7 + a : 25; // 16

2 + (b > a ? b : a); // 6

(a > b ? a : a < b ? b : -1) * (a + 1); // 16
